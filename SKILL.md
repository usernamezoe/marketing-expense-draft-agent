# Expense Resolution Draft Skill

이 스킬은 삼성전자 VD사업부 표준 품의서 작성 프로세스를 가이드하고, 파이썬 코드를 트리거하여 오프라인에서 .docx 문서를 완성함으로써 API 크레딧을 최소화하도록 설계되었습니다.

## ⚙️ 비용 및 크레딧 최적화 가이드 (Token & Credit Optimization)
- **수식 계산용 API 호출 배제**: 증감액 및 증감률 계산 등 수치적 연산은 LLM을 거치지 않고 `docx_generator.py` 내부 파이썬 코드가 독립적으로 처리하여 토큰 소모를 90% 이상 절감합니다.
- **압축된 문맥 전달**: 입력 및 수정 단계를 대화형으로 길게 끌지 않고, 템플릿 기반으로 일괄 입력을 유도하여 컨텍스트 윈도우 누적 크레딧을 보존합니다.

## 📥 입력 파라미터 (Inputs)
| 파라미터명 | 타입 | 필수 여부 | 설명 |
|---|---|---|---|
| `title` | String | 필수 | [메모품의] 타이틀 명칭 |
| `dept` | String | 필수 | 기안 부서명 |
| `date` | String | 필수 | 기안 일자 (YYYY.MM.DD) |
| `background` | Array | 필수 | 추진 배경 및 목적 개조식 문구 리스트 |
| `cost_items` | Array | 필수 | 예산 과목, 전년/금년 금액 리스트 |
| `schedules` | Array | 필수 | 추진 향후 일정 리스트 |

## 📤 출력 파라미터 (Outputs)
- `docx_file`: 생성 완료된 바탕체 포맷의 .docx 파일 다운로드 링크
